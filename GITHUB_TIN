

echo "# app_mern_pcdaklak" >> README.md

git init
--git add README.md
git add .
git commit -m "fix_18032022_1t"
git branch -M main
git remote add origin https://github.com/phandaitin/app_mern_pcdaklak.git
git push -u origin main

…or push an existing repository from the command line

git pull origin main
git remote add origin https://github.com/phandaitin/app_mern_pcdaklak.git
git branch -M main
git push -u origin main


git fetch origin main
Then follow these steps to merge:

git pull origin main
git add .
git commit -m 'your commit message'
git push origin main








 